#include <stdio.h>
int main()
{
    //size of array for user input
    int a;
    scanf("%d",&a);
    //assigning size to the array
    int arr[a];
    //taking input for the array
    for (int i=0;i<a;++i){
        scanf("%d",&arr[i]);
    }
    //printing the array
    for (int j=0; j<a; ++j){
        printf("%d ",arr[j]);
    }
    
    return 0;
}
